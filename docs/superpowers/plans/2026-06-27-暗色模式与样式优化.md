# 暗色模式与样式优化 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（- [ ]）语法来跟踪进度。

**目标：** 为 calc-tools.top 添加暗色模式 + macOS 风格样式优化（保留紫色主色）

**架构：** CSS 变量驱动的主题切换（[data-theme="dark"]），localStorage 持久化 + 系统偏好自动检测。涉及 3 个 CSS 文件、1 个 JS 文件、2 个 HTML 文件。

**技术栈：** 原生 CSS/JS，无外部依赖，仅修改现有文件

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| css/style.css | 修改 | 全局 CSS 变量、header 毛玻璃、按钮、卡片、表单、页脚 |
| css/site.css | 修改 | chip 填充式、标签/热区/点赞/搜索栏暗色适配 |
| css/text-tools.css | 无改动 | 通过 CSS 变量自动继承 |
| js/site.js | 修改 | 追加暗色模式切换逻辑 |
| index.html | 修改 | Header 追加 ☀️/🌙 切换按钮 |
| en/index.html | 修改 | 同上 |

---

### 任务 1：重写 CSS 变量体系

**文件：**
- 修改：css/style.css:1-50（替换 :root 定义）

- [ ] **步骤 1：替换现有 :root 变量为新体系**

将现有的 :root 块（第 1-18 行）替换为新变量体系：

`css
:root {
    /* 主色 */
    --primary: #4f46e5;
    --primary-hover: #4338ca;
    --primary-light: #eef2ff;
    --primary-glow: rgba(79,70,229,0.3);

    /* Light 主题 */
    --bg: #f5f5f7;
    --bg-card: #ffffff;
    --bg-elevated: #ffffff;
    --bg-muted: #f2f2f7;
    --text: #1d1d1f;
    --text-secondary: #86868b;
    --border: #d2d2d7;
    --border-focus: var(--primary);

    /* 阴影层级 */
    --shadow-sm: 0 1px 3px rgba(0,0,0,0.04);
    --shadow-md: 0 4px 12px rgba(0,0,0,0.06);
    --shadow-lg: 0 8px 30px rgba(0,0,0,0.08);
    --shadow-xl: 0 12px 40px rgba(0,0,0,0.12);

    /* 圆角 */
    --radius: 10px;
    --radius-lg: 14px;
    --radius-xl: 20px;
    --radius-full: 999px;

    /* 8px 间距基准 */
    --space-1: 8px;
    --space-2: 16px;
    --space-3: 24px;
    --space-4: 32px;
    --space-5: 40px;

    /* 毛玻璃 */
    --glass-bg: rgba(255,255,255,0.75);
    --glass-border: rgba(255,255,255,0.3);
    --glass-blur: 12px;
}
`

- [ ] **步骤 2：追加 [data-theme="dark"] 变量覆盖块**

在 :root 之后（style.css 中紧跟 :root 后面）添加：

`css
[data-theme="dark"] {
    --bg: #000000;
    --bg-card: #1c1c1e;
    --bg-elevated: #2c2c2e;
    --bg-muted: #2c2c2e;
    --text: #f5f5f7;
    --text-secondary: #98989d;
    --border: #38383a;
    --glass-bg: rgba(0,0,0,0.75);
    --glass-border: rgba(255,255,255,0.1);
    --primary: #818cf8;
    --primary-hover: #6366f1;
    --primary-light: rgba(129,140,248,0.15);
    --primary-glow: rgba(129,140,248,0.4);
    --shadow-sm: 0 1px 3px rgba(0,0,0,0.3);
    --shadow-md: 0 4px 12px rgba(0,0,0,0.4);
    --shadow-lg: 0 8px 30px rgba(0,0,0,0.5);
}
`

---

### 任务 2：Header 毛玻璃 + 切换按钮样式

**文件：**
- 修改：css/style.css（header 区域 ~第 30-50 行）

- [ ] **步骤 1：更新 header 样式为毛玻璃**

将 header 块的 background/border/shadow 替换为：

`css
header {
    background: var(--glass-bg);
    backdrop-filter: blur(var(--glass-blur));
    -webkit-backdrop-filter: blur(var(--glass-blur));
    border-bottom: 1px solid var(--glass-border);
    height: 64px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 var(--space-3);
    position: sticky;
    top: 0;
    z-index: 100;
    box-shadow: var(--shadow-sm);
}
`

- [ ] **步骤 2：添加 theme-toggle 按钮样式**

在 style.css 末尾（或 header 样式附近）追加：

`css
/* ===== Theme Toggle ===== */
.theme-toggle {
    width: 36px;
    height: 36px;
    border-radius: var(--radius-full);
    border: 1px solid var(--border);
    background: var(--bg-card);
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.1rem;
    transition: background 0.2s, border-color 0.2s;
    line-height: 1;
    padding: 0;
}
.theme-toggle:hover {
    border-color: var(--primary);
    background: var(--primary-light);
}
`

---

### 任务 3：工具卡片 & 按钮样式更新

**文件：**
- 修改：css/style.css（工具卡片 ~第 57-90 行，按钮 ~第 100-130 行）

- [ ] **步骤 1：更新 .tool-card-wrap 样式**

将 .tool-card-wrap 的 border-radius 改为 ar(--radius-xl)，替换阴影变量，hover 改为 translateY(-4px)：

`css
.tool-card-wrap {
    position: relative;
    display: flex;
    flex-direction: column;
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
    transition: box-shadow 0.3s ease, transform 0.25s ease;
}

.tool-card-wrap:hover {
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
}
`

- [ ] **步骤 2：更新 .btn 样式**

将 .btn 的 border-radius 和 padding 更新：

`css
.btn {
    flex: 1;
    padding: 14px 28px;
    border: none;
    border-radius: var(--radius-lg);
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.25s ease;
}

.btn-primary:hover {
    background: var(--primary-hover);
    box-shadow: 0 4px 14px var(--primary-glow);
}
`

- [ ] **步骤 3：更新 .hot-tool-card 圆角**

在 site.css 中找到 .hot-tool-card 的 border-radius，改为 ar(--radius-xl)：

`css
.hot-tool-card {
    border-radius: var(--radius-xl);
}
`

---

### 任务 4：Category Chip 改为填充式

**文件：**
- 修改：css/site.css（.category-chip 区域）

- [ ] **步骤 1：替换 .category-chip 样式**

`css
.category-chip {
    padding: 0.45rem 1.2rem;
    border-radius: var(--radius-full);
    border: none;
    background: var(--bg-muted);
    color: var(--text-secondary);
    font-size: 0.85rem;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s;
    user-select: none;
}
.category-chip:hover {
    background: var(--primary-light);
    color: var(--primary);
}
.category-chip.active {
    background: var(--primary);
    color: #fff;
    box-shadow: 0 2px 8px var(--primary-glow);
}
`

---

### 任务 5：全组件暗色适配

**文件：**
- 修改：css/site.css（追加 [data-theme="dark"] 覆盖规则）

- [ ] **步骤 1：追加标签 tag 暗色覆盖**

在 site.css 末尾追加：

`css
[data-theme="dark"] .tag-finance { background: rgba(79,70,229,0.2); color: #a5b4fc; }
[data-theme="dark"] .tag-health { background: rgba(22,163,74,0.2); color: #86efac; }
[data-theme="dark"] .tag-life { background: rgba(202,138,4,0.2); color: #fde68a; }
[data-theme="dark"] .tag-shopping { background: rgba(219,39,119,0.2); color: #f9a8d4; }
[data-theme="dark"] .tag-travel { background: rgba(2,132,199,0.2); color: #7dd3fc; }
[data-theme="dark"] .tag-utility { background: rgba(124,58,237,0.2); color: #c4b5fd; }
[data-theme="dark"] .tag-image { background: rgba(5,150,105,0.2); color: #6ee7b7; }
`

- [ ] **步骤 2：追加点赞按钮暗色覆盖**

`css
[data-theme="dark"] .like-btn {
    border-color: #38383a;
    color: #98989d;
}
[data-theme="dark"] .like-btn.liked {
    background: rgba(244,63,94,0.15);
    border-color: #f43f5e;
    color: #f43f5e;
}
[data-theme="dark"] .detail-like {
    background: var(--bg-card);
    border-color: #38383a;
    color: #98989d;
}
[data-theme="dark"] .detail-like.liked {
    background: rgba(244,63,94,0.15);
    border-color: #f43f5e;
    color: #f43f5e;
}
`

- [ ] **步骤 3：追加热区卡片 & 相关工具暗色覆盖**

`css
[data-theme="dark"] .hot-tool-card {
    background: var(--bg-card);
    border-color: rgba(251,191,36,0.3);
}
[data-theme="dark"] .tool-content {
    background: var(--bg-elevated);
}
[data-theme="dark"] .related-tools {
    background: linear-gradient(135deg, rgba(79,70,229,0.1), rgba(139,92,246,0.1));
    border-color: rgba(79,70,229,0.2);
}
[data-theme="dark"] .related-tools li a {
    background: var(--bg-card);
    border-color: var(--border);
    color: var(--text-secondary);
}
[data-theme="dark"] .related-tools li a:hover {
    border-color: var(--primary);
    color: var(--primary);
}
`

- [ ] **步骤 4：追加页脚暗色覆盖**

在 style.css 中更新 footer 样式，追加暗色覆盖：

`css
footer {
    background: #111113;
    border-top: 1px solid var(--border);
}
[data-theme="dark"] footer {
    background: #0a0a0a;
    border-top: 1px solid #2c2c2e;
}
`

- [ ] **步骤 5：追加全组件过渡动画**

在 style.css 末尾追加：

`css
/* ===== 全局过渡动画 ===== */
body, body * {
    transition: background-color 0.3s ease,
                border-color 0.25s ease,
                box-shadow 0.25s ease,
                color 0.3s ease;
}
`

---

### 任务 6：添加 HTML 切换按钮

**文件：**
- 修改：index.html（header 导航区域 ~第 30 行）
- 修改：en/index.html（对应位置）

- [ ] **步骤 1：在 index.html header nav 中追加 theme-toggle 按钮**

在现有 nav 链接后面（.lang-switch 之前或之后）添加：

`html
<button id="theme-toggle" class="theme-toggle" aria-label="切换暗色模式">
    <span class="theme-icon">☀️</span>
</button>
`

- [ ] **步骤 2：在 en/index.html header nav 中追加同样的按钮**

同上，在 en 版的 header nav 中追加。

---

### 任务 7：添加 JS 切换逻辑

**文件：**
- 修改：js/site.js（末尾追加）

- [ ] **步骤 1：在 site.js 末尾追加暗色模式切换代码**

`js
// ===== 暗色模式切换 =====
(function() {
  const STORAGE_KEY = 'theme-preference';
  const setTheme = (theme) => {
    document.documentElement.setAttribute('data-theme', theme);
    const btn = document.getElementById('theme-toggle');
    if (btn) {
      btn.querySelector('.theme-icon').textContent = theme === 'dark' ? '🌙' : '☀️';
    }
  };
  const saved = localStorage.getItem(STORAGE_KEY);
  if (saved) {
    setTheme(saved);
  } else if (window.matchMedia('(prefers-color-scheme: dark)').matches) {
    setTheme('dark');
    localStorage.setItem(STORAGE_KEY, 'dark');
  }
  document.addEventListener('click', (e) => {
    const toggle = e.target.closest('#theme-toggle');
    if (!toggle) return;
    const current = document.documentElement.getAttribute('data-theme') || 'light';
    const next = current === 'dark' ? 'light' : 'dark';
    setTheme(next);
    localStorage.setItem(STORAGE_KEY, next);
  });
})();
`

---

### 任务 8：验证

- [ ] **步骤 1：本地文件检查**

验证：
1. index.html 和 en/index.html 有 theme-toggle 按钮
2. style.css 有完整的 :root + [data-theme="dark"] 变量体系
3. site.css 有填充式 chip 和暗色覆盖规则
4. site.js 有切换逻辑
5. 所有 CSS 变量引用正确（无 typo）

- [ ] **步骤 2：打开本地 index.html 测试切换**

用浏览器打开 index.html，点击 ☀️/🌙 按钮验证：
- 点击后切换暗色模式
- 刷新后保持偏好
- 所有组件正确渲染

# Baidu Cloud sync files
*.baiduyun.uploading.cfg
*.baiduyun.downloading.cfg

# Brainstorming visual companion
.superpowers/

# 一次性脚本/临时工作区（Phase 1 起归档至此，不进版本库）
.workbuddy/

# OS files
Thumbs.db
.DS_Store

.git_backup
# 知识库文档（存于 Obsidian 知识库躺平计划/projects/2_AI建站项目/docs/）
AGENTS.md
_project_memory.md
使用说明书.md
docs/

# 交付文档（已整体迁至 Obsidian 知识库：02-个人项目/projects/2_AI建站项目/deliverables/）
# 2026-09-23：含 .html 报告、审计截图在内的全部交付物移出工作区
#   —— 远程仓库 thin-Z/calc-tools-top 为 PUBLIC，审计结论与缺陷清单不宜随代码公开
# 保留整目录忽略，防止后续误放回工作区
deliverables/

# 设计预览文件
logo-font-preview.html
logo-preview.html
tag-preview.html

# 无意义文件
项目档案.md
项目进度-*.md

node_modules/

# Playwright (T1.5 E2E) artifacts
test-results/
playwright-report/
blob-report/

# Vercel build output
dist/

# build.mjs 前手工 mv 的 dist 备份（safe-delete 拦截 rmSync(dist) 时的规避手段）
dist.bak*/

# Python
__pycache__/
*.pyc
.env*
!.env.example

# 一次性脚本生成报告
scripts/*_report.txt

snapshots/

# 审计临时目录（诊断脚本/截图/日志，build.mjs 亦已排除出 dist）
.audit_tmp/

# GSC 批提交器临时快照（原在仓库根；2026-09-23 已迁知识库 90-运维工具/reports/，保留规则防再混入）
snap_t.txt

# 搜索引擎提交（Tier 1 · 2026-09-20）：token 与本地推送状态不入库
scripts/.baidu-push-token
scripts/.baidu-push-state.json
scripts/.indexnow-state.json

# 运维产物（2026-09-23 整体迁至 Obsidian 知识库：90-运维工具/reports/）
#   含 GSC 队列(gsc-pending.txt) / 提交日志(gsc-submit-log.md) / 失败证据(gsc-fail/) / 运行输出(gsc_*.out)
#   + 内容度量产物(measure-content.json) + 批提交快照(snap_t.txt)
#   原因：本站仓库 thin-Z/calc-tools-top 为 PUBLIC——索引提交策略与进度、
#        以及「哪些页面内容薄弱」的薄页分布，均不宜随代码公开。
#   保留整目录忽略，防止后续误放回工作区
reports/
